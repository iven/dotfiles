# 交互方式

* 与我交互时默认使用中文。

# 内容生成

* 禁止将对提问者的解释信息写成代码注释。
* 当用户明确要求编写脚本时，应只生成必要的注释和错误处理，以保持代码简洁，不要创建额外的使用说明文件。
* 除了特殊情况外，行尾不应该有空格。
* 每次改动代码时，都集中精力于当前目标。如果发现其他代码不合理的地方，应该在当前目标完成后，询问是否需要改动。

# 工具操作

* 如需创建临时文件，需创建在 /tmp/claude/ 目录下，此目录已存在，直接写入即可。
